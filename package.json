{
  "name": "telegram-worker-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --name telegram-forgecraft-test-bot",
    "deploy:main": "wrangler deploy --name forgecraft",
    "tail": "wrangler tail telegram-forgecraft-test-bot",
    "tail:main": "wrangler tail forgecraft"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "vitest": "~3.2.0",
    "wrangler": "^4.42.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.74.0"
  }
}
